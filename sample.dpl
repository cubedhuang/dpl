// sample fizzbuzz program

fn getString(n) {
	str := "";

	if (n % 3 == 0) {
		str := str + "Fizz";
	};

	if (n % 5 == 0) {
		str := str + "Buzz";
	};

	if (str == "") {
		str := n;
	};

	str;
};

for i := 1 to 100 {
	print(getString(i));
};
